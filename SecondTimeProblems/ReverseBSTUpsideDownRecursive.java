package SecondTimeProblems;

public class ReverseBSTUpsideDownRecursive {
	/*
	 * idea:except subproblem all we need to do is:
	 * 1.newRoot.reverse(root.left);
	 * 2.root.left.left=root;
	 * 3.root.left.rght=root.right;
	 * 4.return newRoot
	 * 
	 */

}
